# UMI VIO 转换器 — H200 使用指南

在 H200 两节点（`jhhuang-h200-qinghua-1` / `-2`，经清华跳板机）上把 UMI 原始 `episode.mcap`
转成带 **VIO 位姿 + 触觉** 的 `episode_with_pose.mcap`。环境已在常驻容器里，**不需要装任何东西**。

> 维护者：elihuang / jhhuang。容器 `tinynav_flatbuffer`（含 ROS2 + CUDA + TensorRT + P1/P2
> 优化：direct-feed + pipeline + MPS + 转换工具）。两节点各有一个同名容器，已长驻运行。

---

## 0. 前提（连接与自检）

```bash
ssh jhhuang-h200-qinghua-1              # 或 -2；经跳板机 ProxyJump
docker ps --filter name=tinynav_flatbuffer     # 应看到容器 Up
docker exec tinynav_flatbuffer nvidia-smi -L   # 应列出 8×H200
```

- 跳板机连接偶发抖动，命令失败**重试**即可；长任务务必放 **tmux**（见 §4）。
- 容器已长驻，**不要 `docker run` 新建**——直接 `docker exec tinynav_flatbuffer ...`。

---

## 1. 关键：节点、路径映射、数据位置

**两节点 `/data` 各自独立**（不共享）；容器内都映射为 `/data`，但**主机路径不同**：

| 节点 | 容器内 | 主机路径（跨节点不同！） | 数据盘 |
|---|---|---|---|
| H200-1 (`-1`) | `/data/X` | `/home/yuchi/shared/datasets/X` | 56T |
| H200-2 (`-2`) | `/data/X` | `/data/shared/datasets/X` | 56T |

- 命令里的路径一律用**容器视角**（`/data/...`）。
- 数据集放在 `/data/shared/datasets/`（容器内 `/data/`）下，如
  `/data/umi_raw_data_260714/<子集>/<hash>/episode.mcap`。
- 从跳板机只读查看某节点产物（直连不稳时）：跳板机 NFS 挂了两节点盘 —
  `/data-214-30-239-40`（=h1 的 /data）、`/data-214-30-239-42`（=h2 的 /data）。

---

## 2. 数据布局

原始 UMI 每集是一个目录，内含两个文件：
```
/data/<数据集>/<子集>/<hash>/
    ├── episode.mcap          # 原始 mcap（6 路 H.264 相机 + IMU + 夹爪 + 触觉）
    └── <hash>.json           # 任务标注 sidecar（task.action_text.rendered_zh/_en 等）
```

---

## 3. 单集快验

```bash
docker exec tinynav_flatbuffer bash -lc '
  export UMI_DIRECT_FEED=1 UMI_PIPELINE=1 CUDA_VISIBLE_DEVICES=0 ROS_DOMAIN_ID=90
  bash /tinynav/tool/umi/umi_vio_converter.sh \
    /data/<数据集>/<子集>/<hash>/episode.mcap \
    --mode auto --pose-sensors left_wrist,right_wrist'
```
- **触觉默认保留**（4 通道），**head 默认不转**（只出左右腕位姿）——团队约定。
- 先看会怎么处理（不动数据）：末尾加 `--mode check`，输出 `action=FULL/BACKFILL/SKIP`。

> ⚠️ 单集直调 `umi_vio_converter.sh` 是**底层入口**：产物**嵌套在原始目录**、**不透传 sidecar
> json**。**要转整个文件夹、产物独立、带任务 json，请用下面 §4 的 `convert.sh`**（推荐）。

---

## 4. 整文件夹转换（推荐入口 `convert.sh`）

**大多数场景：一整个文件夹要转 → 转成一个独立输出文件夹。** 一条命令：

```bash
docker exec tinynav_flatbuffer bash -lc \
  'PAR=64 GPUS=0,1,2,3,4,5,6,7 bash /tinynav/tool/umi/convert.sh \
     /data/<输入文件夹> /data/<输出文件夹>'
```

例：`convert.sh /data/umi_raw_data_260714 /data/umi_vio_data_260714`

- **自动发现**输入下**任意深度**所有 `episode.mcap`，转换后**镜像结构**到输出：
  ```
  /data/<输入>/<子集>/<hash>/episode.mcap
    → /data/<输出>/<子集>/<hash>/episode_with_pose.mcap/   （VIO 位姿 + 触觉）
      /data/<输出>/<子集>/<hash>/<hash>.json               （任务 sidecar，原样透传）
  ```
- **输出独立、输入零污染**：产物只落输出树；转换临时目录**无论成功失败都自动清理**，输入树永远只剩 `episode.mcap` + `<hash>.json`。
- **sidecar `<hash>.json` 自动带上**；**触觉默认保留**；**head 默认不转**。
- **幂等可续跑**：重跑自动 SKIP 已转的，不重复。
- 日志/结果：`<输出>/_vio_logs/results.tsv`（第 2 列 rc，0=成功，非0=失败）。
- **PAR**：独占 8 卡用 `PAR=64`（8/卡）；与人共享时减小。**GPUS**：只填空闲卡号，别占别人的（`nvidia-smi` 先看）。**DBASE**：多任务并行时各用不同基值（`DBASE + PAR ≤ 232`）。

### 长任务放后台（跳板机会断，必须 tmux）
用**主机侧 tmux 包 `docker exec`**（容器内 tmux 会随 exec 结束而死）：
```bash
tmux new-session -d -s conv \
  "docker exec tinynav_flatbuffer bash -lc \
     'PAR=64 GPUS=0,1,2,3,4,5,6,7 bash /tinynav/tool/umi/convert.sh /data/my_raw /data/my_vio' \
   > ~/conv.log 2>&1; echo done > ~/conv.rc"
# 查进度：另开 ssh，看产物增量而非瞬时（VIO 的 GPU 利用是脉冲式的）
docker exec tinynav_flatbuffer bash -lc 'find /data/my_vio -name metadata.yaml | wc -l'
```

### 底层调度器（进阶：自控集列表）
```bash
docker exec tinynav_flatbuffer bash -lc '
  find /data/<数据集> -name episode.mcap > /tmp/eplist.txt
  PAR=64 DBASE=100 GPUS=0,1,2,3,4,5,6,7 RAW_ROOT=/data VIO_ROOT=/data/my_vio \
    bash /tinynav/tool/umi/vio_batch_dispatch.sh /tmp/eplist.txt'
```

### 清点失败集
少数集因原始数据缺陷（`focal_length=0`、缺 `camera_info`）确定性失败、无产物：
```bash
docker exec tinynav_flatbuffer bash -lc \
  'RAW_ROOT=/data/my_raw VIO_ROOT=/data/my_vio SUBSETS="子集1 子集2" \
   bash /tinynav/tool/umi/failed_manifest.sh'
# 输出 <VIO_ROOT>/_vio_logs/failed_manifest.tsv：subset  hash  rc  elapsed_s  error
```

---

## 5. 多节点并行（分布式训练数据）

两节点 `/data` 独立、**无跨节点自动去重**，必须**显式切分集列表**，各转各的、避免重复：

```bash
# 例：h1 转 A_019/A_020/E_001，h2 转 A_001..A_004/A_008（互斥）
# 每节点各自 convert.sh 一个输入子树 → 各自的输出树
```
- 产物留在各自节点（主机路径见 §1），训练时每节点读**自己的**主机路径。
- 别把两节点当共享盘：一个节点看不到另一个的 `/data`。

---

## 6. 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| ssh/scp 偶发 `Connection closed` | 跳板机抖动，**重试**；长任务放 tmux，scp 后 **md5 校验**别假设成功 |
| 容器内 tmux 起不来 / 任务随命令结束就没了 | 用**主机侧 tmux 包 docker exec**（§4），别在容器里起 tmux |
| build_map 秒退 rc=1 | 看 `<输出>/_vio_logs/<tag>.log`；多为标定缺陷（focal=0 / 缺 camera_info），确定性失败、跳过，用 `failed_manifest.sh` 清点 |
| 进度看着像"卡住"（产物+0 / GPU 0%） | VIO 的 GPU 利用是**脉冲式**的（build_map 上 GPU、decode/merge 走 CPU 交替）+ mode auto 秒 SKIP 阶段；用**≥90s 窗口看产物增量**判断，别看瞬时快照 |
| 转换慢、MPS 没生效 | 确认 MPS daemon：`echo get_default_active_thread_percentage \| nvidia-cuda-mps-control`（返回数字=可用）；没起则 `nvidia-cuda-mps-control -d` |
| 与别人抢卡 | `nvidia-smi` 看空卡，`GPUS=` 只填空闲的；别人任务占着别硬挤（显存满会 OOM） |
| 产物想独立、别嵌进原始数据 | 用 §4 `convert.sh`（独立输出树 + 输入零残留 + sidecar 透传） |

---

## 7. 速查

```bash
ssh jhhuang-h200-qinghua-1        # 或 -2

# 整文件夹 → 独立输出（推荐，后台 tmux）
tmux new-session -d -s conv "docker exec tinynav_flatbuffer bash -lc 'PAR=64 GPUS=0,1,2,3,4,5,6,7 bash /tinynav/tool/umi/convert.sh /data/my_raw /data/my_vio' > ~/conv.log 2>&1; echo done > ~/conv.rc"

# 单集快验
docker exec tinynav_flatbuffer bash -lc 'export UMI_DIRECT_FEED=1 UMI_PIPELINE=1 CUDA_VISIBLE_DEVICES=0 ROS_DOMAIN_ID=90; bash /tinynav/tool/umi/umi_vio_converter.sh /data/<数据集>/<hash>/episode.mcap --mode auto --pose-sensors left_wrist,right_wrist'

# 看产物 + 完整性（产物数应 == json 数）
docker exec tinynav_flatbuffer bash -lc 'echo 产物=$(find /data/my_vio -name episode_with_pose.mcap -type d | wc -l) json=$(find /data/my_vio -name "*.json" | wc -l)'
```
