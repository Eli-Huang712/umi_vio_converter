# UMI VIO 转换器 — B300 使用指南（多用户）

在 B300-1（`b300-1` / discover1）上把 UMI 原始 `episode.mcap` 转成带 **VIO 位姿 + 触觉** 的
`episode_with_pose.mcap`。环境已打包成 Docker 镜像，你**不需要装任何东西**。

> 维护者：elihuang。镜像 `tinynav_b300:latest`（含 ROS2 + CUDA + TensorRT + 已针对 B300
> Blackwell 重编的引擎 + 转换工具）。有问题找 elihuang。

---

## 0. 前提（30 秒自检）

```bash
ssh b300-1
id | grep -q ai-users && echo "✅ 你在 ai-users 组，可用 docker" || echo "❌ 不在 ai-users，找管理员加组"
docker images tinynav_b300:latest   # 应看到镜像
```

- 用 docker **不需要 sudo**（docker.sock 对 ai-users 组开放）。
- 你在容器里是 root，但**只操作你自己挂进去的目录**，不会动到别人。

---

## 1. 起一个属于你自己的容器（每人一个，互不干扰）

**关键：把你自己的数据目录挂成容器里的 `/data`。** 别用别人的家目录。

```bash
# 1) 在你家目录建数据目录，放原始 mcap
mkdir -p ~/umi_data

# 2) 起你自己的容器（名字带上你的用户名，避免和别人冲突）
docker run -d --name umi_vio_$USER \
  --runtime nvidia --gpus all --shm-size=16g \
  -v ~/umi_data:/data \
  --restart unless-stopped \
  tinynav_b300:latest
#  ⚠️ 不要在末尾加 sleep infinity —— 镜像自带 keepalive，加了会崩溃循环重启

# 3) 确认容器在跑 + 能看到 GPU
docker ps --filter name=umi_vio_$USER
docker exec umi_vio_$USER nvidia-smi -L
```

**目录映射**：容器里的 `/data/X` == 你主机的 `~/umi_data/X`。下面命令里的路径都写**容器视角**（`/data/...`）。

---

## 2. 放数据

把原始 UMI `episode.mcap` 放到 `~/umi_data/` 下，例如：
```
~/umi_data/<数据集名>/<hash>/episode.mcap
```
容器内即为 `/data/<数据集名>/<hash>/episode.mcap`。

---

## 3. 单集转换

```bash
docker exec umi_vio_$USER bash -lc '
  export UMI_DIRECT_FEED=1 UMI_PIPELINE=1 CUDA_VISIBLE_DEVICES=0 ROS_DOMAIN_ID=90
  bash /tinynav/tool/umi/umi_vio_converter.sh \
    /data/<数据集>/<hash>/episode.mcap \
    --mode auto --pose-sensors left_wrist,right_wrist'
```
- 产物：同目录下 `episode/episode_with_pose.mcap/`（rosbag2 目录包，内含 `*_0.mcap` + metadata.yaml）
- **触觉默认保留**（4 通道），**head 默认不转**（只出左右腕位姿）——这是团队约定，无需额外参数。
- `CUDA_VISIBLE_DEVICES=0`：选你要用的卡号（起容器前先 `nvidia-smi` 看哪张空）。
- `ROS_DOMAIN_ID`：多人/多进程并行时每个要**不同**（0–232），否则 DDS 串扰。

> ⚠️ 单集直调 `umi_vio_converter.sh` 是**原始底层入口**：产物**嵌套在原始目录**里，且**不透传 sidecar json**。**要转整个文件夹、要产物独立、要带任务标注 json，请用下面 §4 的 `convert.sh`**（推荐）。单集直调仅用于快速验证一集。

### 只想先看会怎么处理（不动数据）
```bash
docker exec umi_vio_$USER bash -lc \
  'bash /tinynav/tool/umi/umi_vio_converter.sh /data/.../episode.mcap --mode check'
# 输出 action=FULL / BACKFILL / SKIP
```

---

## 4. 整文件夹转换（推荐入口 `convert.sh`）

**大多数场景：一整个文件夹都要转 → 转成一个独立输出文件夹。** 一条命令：

```bash
docker exec umi_vio_$USER bash -lc \
  'PAR=12 GPUS=0,1 bash /tinynav/tool/umi/convert.sh /data/<输入文件夹> /data/<输出文件夹>'
```

例：`convert.sh /data/my_raw /data/my_vio`

- **自动发现**输入文件夹下**任意深度**的所有 `episode.mcap`，转换后**镜像结构**到输出文件夹：
  ```
  /data/my_raw/<子集>/<hash>/episode.mcap
    → /data/my_vio/<子集>/<hash>/episode_with_pose.mcap/   （VIO 位姿 + 触觉）
      /data/my_vio/<子集>/<hash>/<hash>.json               （任务标注 sidecar，原样透传）
  ```
- **输出独立、输入零污染**：产物只落在输出文件夹，绝不嵌套进原始数据；转换中的临时目录**无论成功失败都自动清理**，输入树永远只剩 `episode.mcap` + `<hash>.json`。
- **sidecar `<hash>.json` 自动带上**（任务指令：`task.action_text.rendered_zh/_en` 等），和产物并排，下游可直接读。
- **触觉默认保留**（4 通道），**head 默认不转**（只出左右腕位姿）。
- **可断点续跑 / 幂等**：重跑自动 SKIP 已转的，不重复。
- 日志/结果：`<输出文件夹>/_vio_logs/results.tsv`（第 2 列 rc，0=成功，非0=失败）。
- **PAR**：每卡约 6 进程（卡数 × 6）；**GPUS**：只填你被分配/确认空闲的卡号。
- 长任务放后台跑：
  ```bash
  tmux new-session -d -s conv "docker exec -e PAR=12 -e GPUS=0,1 umi_vio_$USER \
    bash /tinynav/tool/umi/convert.sh /data/my_raw /data/my_vio > ~/conv.log 2>&1; echo done > ~/conv.rc"
  ```

### 底层调度器（进阶：自己控制集列表时）
`convert.sh` 内部调用 `vio_batch_dispatch.sh`。若你要精确控制转哪些集，可直接喂列表：
```bash
docker exec umi_vio_$USER bash -lc '
  find /data/<数据集> -name episode.mcap > /tmp/eplist.txt
  PAR=12 DBASE=100 GPUS=0,1 RAW_ROOT=/data VIO_ROOT=/data/my_vio \
    bash /tinynav/tool/umi/vio_batch_dispatch.sh /tmp/eplist.txt'
```

### 清点失败集（相机标定缺陷等）
少数集因原始数据缺陷（`focal_length=0`、缺 `camera_info`）确定性转换失败、无产物。生成失败清单：
```bash
docker exec umi_vio_$USER bash -lc \
  'RAW_ROOT=/data/my_raw VIO_ROOT=/data/my_vio SUBSETS="子集1 子集2" \
   bash /tinynav/tool/umi/failed_manifest.sh'
# 输出 <VIO_ROOT>/_vio_logs/failed_manifest.tsv：subset  hash  rc  elapsed_s  error
```

---

## 5. 用完清理

```bash
# 停并删你自己的容器（数据和产物在 ~/umi_data 里，不受影响）
docker rm -f umi_vio_$USER
```
镜像 `tinynav_b300:latest` 是共享的，**不要删**。

---

## 6. 常见问题

| 现象 | 原因 / 解决 |
|---|---|
| `docker: permission denied` | 你不在 ai-users 组，找管理员 |
| 容器一直 Restarting | `docker run` 末尾加了 `sleep infinity`；去掉，用镜像默认 CMD |
| `engine ... incompatible device / compute 9.0` | 你用了别的镜像；确认用 `tinynav_b300:latest`（引擎已按 B300 重编） |
| build_map 秒退 rc=1 | 看 `<输出>/_vio_logs/<tag>.log`（convert.sh/批量路径）；常见是该集标定缺陷（focal=0 或缺 camera_info），确定性失败、跳过即可，用 `failed_manifest.sh` 清点 |
| 转换很慢 | 起 MPS 提速：`docker exec umi_vio_$USER nvidia-cuda-mps-control -d`（可选，约 +15%） |
| 多人同时跑互相干扰 | 各自用不同 `ROS_DOMAIN_ID`（或不同 `DBASE`）和不重叠的 `GPUS` |
| 产物想独立、别嵌进原始数据 | 用 §4 `convert.sh`（默认独立输出树 + 输入零残留 + sidecar json 透传） |

---

## 7. 速查

```bash
# 起容器
docker run -d --name umi_vio_$USER --runtime nvidia --gpus all --shm-size=16g -v ~/umi_data:/data --restart unless-stopped tinynav_b300:latest

# 整文件夹 → 独立输出（推荐）
docker exec umi_vio_$USER bash -lc 'PAR=12 GPUS=0,1 bash /tinynav/tool/umi/convert.sh /data/my_raw /data/my_vio'

# 单集快验
docker exec umi_vio_$USER bash -lc 'export UMI_DIRECT_FEED=1 UMI_PIPELINE=1 CUDA_VISIBLE_DEVICES=0 ROS_DOMAIN_ID=90; bash /tinynav/tool/umi/umi_vio_converter.sh /data/<数据集>/<hash>/episode.mcap --mode auto --pose-sensors left_wrist,right_wrist'

# 看产物（独立输出树）
find /data/my_vio -name metadata.yaml | head ; find /data/my_vio -name '*.json' | wc -l
```
