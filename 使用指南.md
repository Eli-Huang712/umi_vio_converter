# umi_vio_converter 使用指南

给 tinynav 的 UMI VIO 转换做手术刀级改动，让 `*_with_pose.mcap` 原生带上**触觉**，
位姿按传感器可开关，并对已有产物**只补差不重跑 VIO**。本指南覆盖日常所有操作。

> 背景与设计见 [DESIGN.md](DESIGN.md)，速度数据见 [BENCHMARK.md](BENCHMARK.md)。

---

## 0. 三种工作模式（先理解这个）

工具按每集当前状态**自动分派**（`--mode auto`，默认）：

| 产物状态 | 动作 | 代价 |
|---|---|---|
| 还没有 `*_with_pose.mcap` | **FULL**：跑 VIO 建图 → 合并（带触觉） | GPU |
| 有 `with_pose` 但无触觉 | **BACKFILL**：只补触觉，不碰 VIO/位姿 | 纯 CPU，秒级 |
| 有 `with_pose` 且已带触觉 | **SKIP**：幂等跳过 | 0 |

**存量产物补触觉 = BACKFILL，不需要 GPU、不重跑 VIO。** 只有连 `with_pose` 都没有的
raw 集才走 FULL。

---

## 1. 前置：容器 ↔ 主机路径映射（最容易踩的坑）

代码跑在 H200-1 的容器 `tinynav_flatbuffer` 里，容器把主机数据盘挂到 `/data`：

```
容器  /data/<X>   ==  主机  /data/shared/datasets/<X>   （== /home/yuchi/shared/datasets/<X>）
```

例：主机 `/data/shared/datasets/plant_collection/raw/...`
→ 容器内要写成 `/data/plant_collection/raw/...`（**去掉 `shared/datasets`**）。

所有 `docker exec` 命令里的路径都用**容器视角**（`/data/...`）。

---

## 2. 部署 / 回滚 / 固化（在 H200-1 主机上）

代码在本机开发，rsync 到主机后用 `deploy.sh` 装进容器。改动只覆盖 3 个原文件
（`flatbuffer_codec/reader/merge`），装前会**快照整镜像 + 备份原文件**，验证通过前**不动 `:latest`**。

```bash
# 本机 → 主机
rsync -av umi_vio_converter/ h1:/tmp/umi_vio_converter/

# 主机上：装进容器（备份镜像 + 备份原文件 + 拷入 + 语法检查）
ssh h1
cd /tmp/umi_vio_converter
bash deploy.sh deploy   tinynav_flatbuffer     # 部署（live，未提交 latest）
# ...验证一集（见 §5）...
bash deploy.sh finalize tinynav_flatbuffer     # 通过后再 docker commit :latest
bash deploy.sh rollback tinynav_flatbuffer     # 需要时一键还原原文件
```

> 当前容器里**已部署**这套代码（备份镜像 `tinynav_flatbuffer_saved:backup-20260707-162631`），
> 尚未 `finalize` 到 `:latest`。

---

## 3. 单集操作

统一入口是 `umi_vio_converter.sh`（自动 source ROS、确保 python-mcap，再调编排器）：

```bash
# 自动模式：按状态决定 FULL / BACKFILL / SKIP
docker exec tinynav_flatbuffer bash tool/umi/umi_vio_converter.sh /data/<...>/episode.mcap

# 只看会走哪条分支，不动数据（先摸清工作量）
... umi_vio_converter.sh /data/<...>/episode.mcap --mode check

# 强制只补触觉（已有 with_pose）
... umi_vio_converter.sh /data/<...>/episode.mcap --mode backfill

# 只出左腕位姿 / 只转换不加位姿（都仍带触觉）
... umi_vio_converter.sh /data/<...>/episode.mcap --pose-sensors left_wrist
... umi_vio_converter.sh /data/<...>/episode.mcap --pose-sensors ""
```

### 只补触觉也可直接调底层脚本（更轻，纯 CPU）
```bash
docker exec tinynav_flatbuffer /opt/venv/bin/python3 \
  tool/umi/backfill_tactile.py \
  --with-pose /data/<...>/episode/episode_with_pose.mcap \
  --raw       /data/<...>/episode.mcap
```

---

## 4. 批量处理

### 4a. 批量补触觉（存量产物，纯 CPU，推荐）

用仓库自带的 `batch_backfill.sh`，并行、幂等、可断点续跑、非破坏：

```bash
# plant_collection 布局：<hash>/episode.mcap
docker exec tinynav_flatbuffer bash -lc \
  "PAR=32 bash /tinynav/tool/umi/batch_backfill.sh \
   /data/plant_collection/raw/26-06-23/dataloop-umi/184"

# pick_tiny_objects 布局：<hash>/<hash>.mcap（用 RAW_GLOB 指定）
docker exec tinynav_flatbuffer bash -lc \
  "PAR=32 RAW_GLOB='*/*.mcap' bash /tinynav/tool/umi/batch_backfill.sh \
   /data/pick_tiny_objects/vio_todo"
```

- **实测**：plant_collection 5021 集，PAR=32，**124 秒**跑完（~0.77s/集），CPU 负载仅 20/192。
- 先小跑试水：加 `LIMIT=20`。
- 结果统计在 `<根目录>/../backfill_logs/results.tsv`（`OK/SKIP/FAIL/NO_WP` + 每集耗时 + 插入条数）。
- ⚠️ `batch_backfill.sh` 里 with_pose 路径默认按 `<raw目录>/episode/episode_with_pose.mcap`
  推导；若你的布局不同，改脚本里 `worker()` 那一行 `WP=...` 即可。

### 4b. 批量 FULL（需要 GPU）

FULL 走团队现成的批处理调度器，把它的容器内执行脚本指向本工具入口即可：

```bash
VIO_RUNNER_IN_CONTAINER=/tinynav/tool/umi/umi_vio_converter.sh \
VIO_MAX_PARALLEL=48 VIO_GPU_IDS="0,1,2,3,4,5,6,7" \
  bash run_vio_batch_retry_parallel.sh /path/to/vio_todo
```

`--mode auto` 让它对已完成集自动走 BACKFILL/SKIP、只对缺 with_pose 的集跑 GPU。

**并行度建议（8×H200 实测，见 BENCHMARK.md）**：
- **可持续推荐 PAR=48**（每卡 6 进程）≈ **1077 集/时**，CPU 56%、显存充裕，共享节点也安全。
- **绝对上限 PAR=64**（每卡 8）≈ 1188 集/时，但 CPU 92% 逼近饱和 → **仅独占节点**。
- 再高会崩：见 §7 的 `ROS_DOMAIN_ID ≤ 232` 坑。

---

## 5. 验证产物

用公共 venv（自带 mcap，无需进容器）：

```bash
/data/shared/tools/ngad_viz/venv/bin/python - <<'PY'
from mcap.reader import make_reader
import glob, re
T = re.compile(r"tactile_point_cloud2$")
wp = "/data/shared/datasets/<...>/episode/episode_with_pose.mcap"
f = sorted(glob.glob(wp + "/*.mcap"))[0]
s = make_reader(open(f, "rb")).get_summary()
c = s.statistics.channel_message_counts
tac = sorted((ch.topic, c[cid]) for cid, ch in s.channels.items() if T.search(ch.topic))
print("触觉通道:", len(tac))
for t, n in tac: print(" ", n, t)
print("总通道:", len(s.channels))
PY
```

**判定标准**：
- 触觉通道数 = raw 里实际存在的路数（满夹爪 4 路，残缺集 2 或 3 路）；每路计数与 raw 对应通道一致。
- 视频/IMU/夹爪/位姿/camera_info 与补差前**逐字节不变**（BACKFILL 保证；可与 `.pre_tactile.bak` 对比）。
- 触觉话题保持原名 `/observation/tactile_..._point_cloud2`，类型 `sensor_msgs/msg/PointCloud2`
  （字段 `x,y,z,fx,fy,fz`，`width` = taxel 点数）。

---

## 6. 备份管理

BACKFILL 每改一集会在原地留 `episode_with_pose.mcap.pre_tactile.bak`（原产物副本）。

```bash
# 抽查确认无误后，批量删除备份释放空间（plant_collection 约 160GB）
find /data/shared/datasets/plant_collection/raw/26-06-23/dataloop-umi/184 \
     -maxdepth 4 -type d -name "*.pre_tactile.bak" -exec rm -rf {} +
```

> 建议先抽查几集（§5，比对 `.pre_tactile.bak`）再删。

---

## 7. 两个必知的坑（已在工具内处理，批量时仍需注意）

1. **`ROS_DOMAIN_ID ≤ 232`（FastDDS 硬上限）。** 并行 FULL 时每个 worker 用独立 domain，
   `DOMAIN_BASE + 并行数` 不能越过 232，否则 DDS 实例秒崩（"domainId over 232"）。
   团队调度器默认 base=70，并行到 ~160 都安全。
2. **tmux pane 不继承 `docker exec` 的环境。** `CUDA_VISIBLE_DEVICES` 已在
   `_build_map_one_sensor.sh` 里重新 export 进 tmux 命令，否则并行 FULL 会全挤到 GPU 0。
   —— 这条已修好，无需你操作，仅说明原理。

---

## 8. 故障排查

| 现象 | 原因 / 处理 |
|---|---|
| `No such file or directory`（路径对但访问不到） | 路径映射错了 → 容器内用 `/data/...`（§1） |
| `action=skip` 但你想重转 | 幂等保护；补触觉重建用 `--force`，FULL 重跑用 `--mode full`（会覆盖 VIO，先备份） |
| FULL 秒崩、日志有 `RTPS_TRANSPORT_SHM` / `domainId over 232` | 并行度 + DOMAIN_BASE 越过 232（§7） |
| 并行 FULL 全堆在 GPU 0 | 用的不是本工具的 `_build_map_one_sensor.sh`（缺 CUDA 重导出，§7） |
| BACKFILL 报 `no metadata.yaml` | 该集没有 with_pose 产物 → 需要先跑 FULL |
| 某集触觉只有 2/3 路 | 正常：该 raw 本身缺整只夹爪或单指（~9.6% 的集），只补实际存在的通道 |

---

## 9. 一句话速查

```bash
# 存量补触觉（纯 CPU，最常用）
PAR=32 bash /tinynav/tool/umi/batch_backfill.sh <容器内根目录>

# 单集自动处理
bash /tinynav/tool/umi/umi_vio_converter.sh /data/<...>/episode.mcap

# 只看不动
... umi_vio_converter.sh /data/<...>/episode.mcap --mode check
```
