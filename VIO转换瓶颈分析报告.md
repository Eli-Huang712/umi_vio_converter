# tinynav UMI VIO 转换：吞吐瓶颈分析与优化报告

> **对象**：tinynav UMI VIO 转换流水线（raw `.mcap` → `*_with_pose.mcap`，为每集机器人数据补出位姿轨迹）
> **问题**：单节点批量转换的吞吐被卡在某个平台上，但不知道被什么卡住、天花板多高、怎么突破。
> **方法**：CPU/GPU 系统性剖析（cgroup CPU-busy、mpstat、nvidia-smi dmon 引擎活跃度、同卡多进程时间线、py-spy 火焰图、NVIDIA MPS A/B）。
> **日期**：2026-07-08 ｜ 剖析节点：H200（8×NVIDIA H200 143GB + 192 CPU 核）

---

## 摘要

批量 VIO 转换的单节点吞吐平台是 **PAR=64 时约 1196 集/时**（≈ 54.5 万帧/时）。本报告用真实利用率指标证明：**这不是任何计算资源的硬墙**——峰值处 CPU 只用到算力上限的 ~40%、GPU 引擎活跃度仅 7%、显存/IO 带宽余量在 20× 以上。

经过对三个候选瓶颈的逐一测试，**真正的瓶颈被定位为 `build_map` 进程的单线程 ROS2 消息序列化**：每处理一帧图像，进程要在纯 Python 里逐字节拷贝一遍 CDR 图像消息，这段代码受 GIL 约束、把每个进程钉死在约一个 CPU 核上。因为每个进程既吃不满多核、又几乎不用 GPU，节点在进程数堆到 ~64 时就到顶，两种计算资源都还闲着。

一个被广泛看好的候选——**NVIDIA MPS**（多进程共享 GPU context）——经同卡 N=8 实测 A/B **收益为零**（421→420 腕/时，误差内），因为瓶颈根本不在 GPU。这纠正了此前"GPU-context 时间片串行化是主因"的假设。

**优化建议**按收益/风险排序：① 零代码的编排修复（`--shm-size` + 锁定 PAR=64）立刻回收被浪费的算力；② 削减 ROS 图像序列化开销是抬高单节点上限的正确方向（预期 +25–50%）；③ MPS 与常驻 worker 均经实测否决。

> **关于吞吐单位**：本报告主用"帧/时"而非"集/时"。UMI 每集录制时长基本恒定（~11s），但**帧数随内容在 73–461 帧/腕间变化**（中位 228），真实计算量随帧数走、与集大小无关。故"集/时"会被集大小分布干扰，"帧/时"才是尺寸无关的物理量。为与历史数据衔接，关键处并列给出两者（中位集 ≈ 456 帧 = 双腕）。

---

## 1. 流水线解剖：一集数据是怎么被转换的

理解瓶颈前，先看清一集的处理链路。每个 raw 集包含双腕 + 头部相机的 H.264 视频、IMU、夹爪、触觉。VIO 转换要为**两只手腕**各跑一遍视觉惯性里程计，产出位姿轨迹，再合并回一个带位姿的 mcap。

```
一集 raw.mcap
   │
   ├── build_map(left_wrist)  ┐  每腕 = 两个 ROS 进程：
   │                          │    perception_node ── (ROS topics) ──> build_map_node
   ├── build_map(right_wrist) ┘    经定速回放(play_rate=20)驱动，逐帧处理
   │
   └── merge_sqlite_mcap ──> episode_with_pose.mcap（带触觉）
```

**单腕内部的两进程架构**（这是后面理解瓶颈的关键）：
- **`perception_node`**：加载 TensorRT 模型（SuperPoint 特征点 + LightGlue 匹配 + 立体深度），对每帧做特征提取与匹配。**多线程**（TRT 推理 + asyncio 立体 worker）。
- **`build_map_node`**：用 `BagPlayer` 定速回放原始 bag，把每帧图像/IMU 经 ROS topic 喂给感知，收回结果做位姿图优化、回环、occupancy 建图。
- 两者经 ROS2 DDS 通信（FastDDS，默认走共享内存传输）。

**单腕耗时拆解**（实测，一集中位）：

| 阶段 | 耗时 | 说明 |
|---|---|---|
| 进程 spawn + CUDA/ROS 初始化 + DDS 发现 | ~1s | 固定开销 |
| TRT 模型加载（3 个 .plan 反序列化） | ~1s | 每进程重付 |
| **逐帧感知（定速回放 + 特征/匹配/深度/SLAM）** | **~29s** | **主导项**，228 帧 × ~127ms/帧 |
| └─ 其中真正的建图数学（位姿图/回环/occupancy） | 仅 1.8s | profiler "Grand total" |

**第一个关键观察**：一集只录了 ~11s、~228 帧/腕，处理却要 ~34s/腕，**时间几乎全花在逐帧感知的 29s 上，而真正的建图数学只有 1.8s**。所以任何"摊销固定启动开销"的思路（模型加载、spawn 只占 ~2s）天花板都极低——这也是为什么常驻 worker 方案最终只有 ~15% 收益（§5）。

**第二个关键观察**：VIO 输出**不是逐次可复现的**。定速回放 + 异步多线程感知对调度抖动敏感，`ApproximateTimeSynchronizer` 在不同时序下会把 stereo/IMU 帧配成不同对，导致同一集重跑得到不同的关键帧数与位姿。这意味着"逐字节一致"不能作为任何提速方案的验收闸门——一个贯穿全程的约束。

---

## 2. 谜题：吞吐平台在哪，为什么它不该存在

### 2.1 并行度扫描：平台是 PAR≈64、约 1196 集/时

在 8 卡空闲节点上，以并行度 PAR（同时转换的集数，分摊到 8 卡）做吞吐扫描。**每档都测真实利用率**——CPU 用 mpstat 的 `%idle`（而非 loadavg），GPU 用 nvidia-smi dmon 的 `sm%` 引擎活跃度（而非 `utilization.gpu`）：

| PAR | 吞吐(集/时) | 吞吐(万帧/时) | 真实 CPU-busy(稳态) | GPU 引擎 sm%(稳态) | 失败 |
|---:|---:|---:|---:|---:|---:|
| 8  | 275  | 12.5 | ~8% | ~2% | 0 |
| 32 | 829  | 37.8 | 33% | 5% | 0 |
| **64** | **1196** | **54.5** | **50%** | **7%** | 0 |
| 80 | 953 ↓ | 43.5 | 48% | 7% | 3 |
| 96 | 936 ↓ | 42.7 | 51% | 7% | 4 |

**吞吐在 PAR=64 见顶（1196 集/时），PAR=80/96 不升反降**（953/936，−20%），且开始出现进程崩溃。这条曲线（图2）把此前"边际增益线性外推、约 PAR=67 归零"的**推断**，变成了**实测的峰 + 回退**。

> **方法学纠错（重要）**：初版分析曾据 loadavg=177/192 与 `nvidia-smi utilization.gpu`=80% 断言"CPU 瓶颈、已饱和"。**这两个指标都不能证明饱和**：loadavg 含不可中断（D 态）睡眠的线程（等 GPU/磁盘/DDS 的进程也计入，却不占 CPU 核）；`utilization.gpu` 是"有 ≥1 个 kernel 驻留的时间占比"，132 个 SM 里跑 1 个也读作 100%。本报告一律用 mpstat `%idle` 与 dmon `sm%`（真实引擎活跃度）。

### 2.2 平台不该存在：四条 roofline 没有一条被撞到

用真实资源足迹算出每种资源的吞吐上限（roofline，图1）：

| 资源 | 上限 | 口径（全部实测） |
|---|---:|---|
| **观测平台** | **1196 集/时** | PAR=64 实测峰 |
| CPU 计算 | 2949 集/时 | 192 核 × 3600 / **234 核-秒/集**（cgroup `cpu.stat` 实测） |
| GPU 计算 | ~24000 集/时 | 8 卡 × 3600 / **1.2 GPU-秒/集**（同卡多进程 sm% 积分反推） |
| IO 带宽 | ~66500 集/时 | **4.6 GB/s** 盘写（dd 实测）× 3600 / 249MB 写/集 |
| 显存 | 从不瓶颈 | 峰值 ~20/143 GB（每进程仅 2.4GB 模型常驻） |

**观测平台 1196 只有最近那堵墙（CPU 计算 2949）的 41%，离 GPU/IO 更是一两个数量级。** 帧归一化后结论不变：观测 54.5 万帧/时 vs CPU 上限 136 万帧/时（0.51 核-秒/帧），同样是 40%。

**这就是谜题**：没有任何一种计算/IO/显存资源被撞到硬墙，可吞吐就是上不去、单集延迟还随并行度一路膨胀（PAR 1→64 时单集从 78s 涨到 193s）。有一堵"软墙"存在——**它限制的是每个进程的效率，而非任何全局资源总量**。

---

## 3. 追凶：三个假设，逐一测试

面对"软墙"，有三个候选嫌疑。本节逐一给出**决定性实验**。

### 假设 A：GPU-context 时间片串行化（此前的头号嫌疑）

**假设**：PAR=64 时每卡挤了 8 个进程，各自的 CUDA kernel 要挤过同一张卡的 context，被时间片轮流串行执行——于是聚合 GPU 只用了一半、但每进程延迟翻倍。

**决定性实验（同卡多进程）**：在**一张空闲卡**上并发跑 N 个单腕转换（N=1,2,4,6,8），看聚合引擎活跃度、每进程延迟、吞吐怎么随 N 变。**这个实验判定力极强**：N≤8 时这些进程总共只用 ≤25/192 核，CPU 绝无可能是混淆项——任何延迟膨胀都只能来自 GPU 侧。

| N（同卡进程数） | 整组墙钟 | 每进程延迟 | 吞吐(腕/时) | GPU sm% |
|---:|---:|---:|---:|---:|
| 1 | 34.3s | 34.3s | 105 | 2% |
| 2 | 67.2s | 50.9s | 107 | 2% |
| 4 | 67.3s | 42.1s | 214 | 5% |
| 6 | 68.5s | 48.4s | 315 | 7% |
| 8 | 68.4s | 48.1s | **421** | 7% |

**判读**：若真是 context 串行化，应看到"sm% 平、每进程延迟随 N 线性涨、吞吐平"。**实测恰恰相反**——从 N=2 到 N=8，**整组墙钟几乎不变（~68s）而吞吐近线性涨（107→421，~4×）**：8 个进程塞进了和 2 个进程一样的墙钟里。**一张卡能从容吸收 ~8 个并发 VIO 进程，GPU 不是那堵墙**（图5）。

**钉死它——NVIDIA MPS A/B**：MPS 让多进程共享同一个 GPU context，正是消除"context 时间片串行化"的专用工具。在 N=8 同卡、同一批集上关/开 MPS 对比：

| | 吞吐(腕/时) | GPU sm% | 每进程延迟 |
|---|---:|---:|---:|
| MPS 关 | **421** | 7% | 48.1s |
| MPS 开 | **420** | 7% | 48.0s |

**MPS 收益 = −0.2%（统计上为零）**（图7）。如果瓶颈真是 GPU-context 串行化，MPS 必然显著提升；**零提升，假设 A 被证伪**。MPS 什么都没做，是因为根本没有 context 争用可消除——N=8 时 GPU 引擎才 7% 活跃，卡是空的。

> **这是本轮相对初版分析最重要的更正**：初版据"单进程延迟翻倍 + 聚合 GPU ~50%"把 GPU-context 串行化列为头号主因、把 MPS 列为首选优化。同卡时间线 + MPS A/B 两个实验联手证伪了它。

### 假设 B：`/dev/shm` / DDS 传输耗尽

**假设**：ROS2 的 FastDDS 默认走共享内存传输，每个 participant 在 `/dev/shm` 分配段；高并行度下段数暴涨。

**实验**：直接观察高 PAR 下的 `/dev/shm`。容器默认 `/dev/shm` 只有 **64MB**，在 PAR≥32（约 64+ 个 DDS participant）时被**耗尽** → `RTPS_TRANSPORT_SHM: Failed to create segment`，**约 40% 的集失败、且失败率随 PAR 增长**。

**结论**：这是一个**真实存在、但被掩盖**的软瓶颈。生产批处理驱动 `run_vio_batch_retry_parallel.sh` 靠**自动重试**掩盖它——失败的集重跑一遍，白白烧掉一份 GPU/CPU，**直接压低有效吞吐**却不报错。**它是次要因素**（限制的是高 PAR 的稳定性与有效吞吐，不是 PAR=64 峰本身），但**修复零成本**：`docker run --shm-size=16g`（宿主 2TB RAM），修复后 PAR=32 时 `/dev/shm` 仅用 1%、零失败。本报告所有干净数据都是在修复后测得的。

> 顺带解释了 PAR=80/96 的崩溃：那 3–4 次失败栈一致，都在 `build_map_node.py:610 wait_for_perception_subscribers → ExternalShutdownException`——高过订下感知-建图的 DDS 订阅握手变得不稳定。这也是"PAR>64 得不偿失"的一部分。

### 假设 C：每进程单线程 CPU 关键路径（真凶）

排除了 GPU（假设 A）、把 DDS 归为次要（假设 B）后，逻辑上瓶颈只能在**单进程的关键路径**里：既然 CPU 总量不满（50%）、GPU 不满（7%），那一定是**每个进程自己跑得不够快、且这种"慢"不随加核而缓解**。

**决定性实验（CPU 火焰图 + 每线程 %CPU）**：用 py-spy 对稳态下的 perception 与 build_map 进程采样，并用 `pidstat -t` 看每线程 CPU 占用。

**build_map 火焰图热点**（图6，吞吐限制者）：
- **`sensor_msgs/msg/_image.py:267-268` 的 `Image.data` getter + 生成器表达式 = 34% + 29%** —— 每条图像 ROS 消息都在**纯 Python 里跑一个生成器表达式逐字节拷贝** image data 的 CDR (反)序列化。
- `rclpy/executors.py` 的 spin_once / wait_for_ready_callbacks = 17% —— ROS 事件循环。
- `flatbuffer_reader._generate` + `decode_compressed_video` = 11% —— 定速回放的解码。

**每线程 GIL 证据**（`pidstat -t`，18s 中位）：

| 进程 | 进程总 %CPU | 最忙单线程 | 全线程和 | top/sum |
|---|---:|---:|---:|---:|
| **build_map** | 89.5% | 68% | 74% | **0.92** |
| perception | 97.5% | 4.5% | 73% | 0.06 |

- **build_map: top/sum = 0.92 → 彻底 GIL 串行**。一个线程（68% CPU）干了几乎全部的活，进程被**钉在约 1 个核**上——它正卡在上面火焰图那段 ROS 图像序列化里。
- perception: top/sum = 0.06 → 真多线程（TRT 推理 + asyncio 立体 worker 真并行），**不是**串行瓶颈。

**真凶确认：`build_map` 进程的单线程 ROS2 图像消息序列化。** 每处理一帧，进程要在 GIL 保护的纯 Python 里逐字节搬一遍图像数据，加上 rclpy 事件循环开销——这段占了 build_map ~63% 的时间，且**无法靠多核并行**（GIL）。

---

## 4. 一个机理解释所有现象

"build_map 被单线程 ROS 消息序列化钉在 ~1 核"这一个机理，把前面所有看似矛盾的现象串成了一条线：

| 现象 | 机理解释 |
|---|---|
| CPU 只用到 50%、PAR=64 就到顶 | 每个 build_map ≈ 1 核（GIL 串行），~128 个 build_map 进程 + perception 的多线程份额 ≈ 填满 ~64 并行度就用完了"有效"调度空间，远不到 192 核的算力上限 |
| GPU 引擎只 7% | build_map 几乎不碰 GPU，它在搬消息；perception 用 GPU 但被上游喂数据的速度限制 |
| MPS 零收益 | 没有 GPU-context 争用可消除——瓶颈在 CPU 侧的消息路径 |
| 单集延迟随 PAR 膨胀（78→193s） | 更多进程 → 定速回放 + DDS + 单线程序列化路径上争用加剧，每进程被拖慢 |
| PAR>64 吞吐回退 | 过订使每进程延迟膨胀超过并行收益，聚合吞吐反降；同时 DDS 握手不稳导致崩溃 |
| 常驻 worker 只有 ~15% 收益 | 它摊销的是启动开销（~2s/集），没碰主导项（29s 逐帧感知里的消息序列化） |

**真正的"绑定资源"不是 CPU 总量、GPU、IO 或显存中的任何一个，而是"每进程单线程消息搬运"这条效率软墙。** 这解释了为什么加核（横向堆资源）在单节点内无效——瓶颈是每进程的串行段，不是资源总量。

---

## 5. 优化方案：按收益/风险/工作量排序

| 优先级 | 方案 | 预期收益 | 风险 | 工作量 | 依据 |
|---|---|---|---|---|---|
| **P0** | `--shm-size=16g` + **锁定 PAR=64** | 消除 DDS 失败（回收被 retry 白烧的算力）+ 避免 PAR>64 的 −20% 回退 | 无（纯编排） | 极低 | §3-B、§2.1 实测 |
| **P1** | **削减 ROS 图像消息序列化开销** | 单节点 **+25–50%**（→ ~1500–1800 集/时） | 中（改 build_map 的 I/O 路径，**不动 VIO 数学**） | 中高 | §3-C 火焰图：序列化占 build_map ~63% |
| P2 | 存量集走 BACKFILL 而非 FULL | 存量补触觉纯 CPU、~0.77s/集、不占 GPU（对存量快 ~50×） | 无 | 已实现 | 已交付 |
| ✗ | ~~NVIDIA MPS~~ | **实测 0%，否决** | — | — | §3-A A/B：421→420 |
| ✗ | ~~常驻 worker~~ | ~15% 上限 + 无法保证位姿正确，否决 | 高（静默损坏位姿） | — | §1 主导项分析 |

### P0：零代码立刻拿的收益（先做这个）
两条纯编排修改，不碰任何代码：
1. **`--shm-size=16g`**：消除高 PAR 下的 DDS 段耗尽。当前 stock 驱动靠 retry 掩盖失败，等于持续白烧算力；修复后失败归零，有效吞吐立刻回收。
2. **锁定 PAR=64**（不要过订到 80/96）：PAR=64 是实测峰（1196），过订反而 −20% 且引入崩溃。若节点为共享，PAR=48 更稳（约 96% 峰值、抗 co-tenant）。

### P1：抬高单节点上限的正确方向

> **✅ 已实现（2026-07-08，`feature/p1-direct-feed`）——详见 [`P1_IMPLEMENTATION.md`](P1_IMPLEMENTATION.md)。**
> 采用"进程内同步喂帧"：单进程把解码后的 numpy 帧点对点喂给 perception+build_map，绕开
> `sensor_msgs/Image` 逐字节 CDR + rclpy executor。**实测（h2，128 集稳态 PAR 扫描，帧/时口径）**：
> **PAR=64 同档 600K → 1.61M 帧/时 = +169%（2.69×），零失败**，远超 +25–50% 目标；机理是
> direct-feed 同时解放了被 GIL 卡住而闲置的 CPU(33%→57%)**和** GPU(sm 5%→60%)。核-秒 −34~36%
> （roofline 下界 +51~55%），平均占用核 1.9→4.7（GIL 软墙消失），墙钟 3.25~3.7×。PAR 扫描 32/48/64/80/96/128
> = 1.27M/1.53M/**1.61M**/1.56M/1.12M/1.20M 帧/时：**df 在 PAR=64 见顶后回退**（同 stock 形状、天花板高 2.69×），
> 故 PAR=64 是最优点（与报告为 stock 锁定的 PAR 一致，无需改编排）。
> 通道 parity（触觉/IMU/夹爪/camera_info）语义完全一致。轨迹：4/6 亚毫米、
> direct-feed 逐次完全确定（顺带修了非确定性）；2/6 动态 right_wrist 偏 ~13mm（机理=不丢帧的帧超集，
> handoff §4 允许的"不同但确定"轨迹，待团队判定是否收紧）。

火焰图把靶子指得很明确：`sensor_msgs/msg/_image.py` 的 `Image.data` 用生成器表达式逐字节拷贝图像。可行方向（均需在**不改 VIO 数学**、并接受"VIO 本就逐次抖动、用轨迹级容差验收"的前提下验证）：
- 用 rclpy 的 **raw/serialized message 路径**或 **numpy 零拷贝**替换逐字节 data getter（ROS2 Python 图像序列化慢是社区已知问题）。
- 把定速回放的**解码**（`decode_compressed_video`）与感知计算**流水线化/解耦**，缩短单线程串行段。
- 目标：把 build_map 的单进程 CPU 关键路径（0.51 核-秒/帧）砍下来，直接抬高 CPU roofline 并把 PAR 峰值右移。**这是唯一能真正突破单节点软墙的路径**——因为它直击"每进程效率"，而非资源总量。

### 明确否决
- **NVIDIA MPS**：实测 A/B 收益为零（瓶颈不在 GPU）。这是任务要求必测的一枪，结论是**打空**。
- **常驻 worker**：即便修好跨集状态泄漏，天花板也只有 ~15%（逐帧感知不可摊销），且有静默损坏科研位姿的风险。
- **提高 play_rate**：喂更快很可能丢帧、劣化位姿，且 VIO 非确定性使其难以干净验证。

---

## 6. 结论

1. **瓶颈是什么**：不是 GPU-context 串行化（此前头号假设，**实测证伪**），而是 **`build_map` 进程的单线程 ROS2 图像消息序列化**——GIL 把每个进程钉在 ~1 核，火焰图显示 63% 时间在 `sensor_msgs/_image.py` 的逐字节 CDR 序列化 + rclpy 事件循环。
2. **上限多高**：观测平台 1196 集/时（54.5 万帧/时）只是最近的 CPU 墙（2949 集/时 / 136 万帧/时）的 41%，GPU/IO/显存余量都在 20× 以上——**没有任何计算硬墙被撞到**，是一堵限制每进程效率的软墙。
3. **怎么突破**：P0 零代码编排修复（`--shm-size` + 锁 PAR=64）立刻回收被浪费的算力；P1 削减 ROS 图像序列化是抬高单节点上限的正确方向（预期 +25–50%）；MPS 与常驻 worker 均经实测否决。

---

## 附录 A：方法与工具

- **真实利用率**：CPU 用 cgroup v2 `cpu.stat` 的 `usage_usec` 前后取差（核-秒）+ mpstat `%idle`；GPU 用 nvidia-smi dmon `sm%`（引擎活跃度）。**不用** loadavg / `utilization.gpu`（前者含 D 态等待、后者是 kernel 驻留率，均不能证明饱和）。
- **火焰图**：py-spy 采样（无需改码）。本容器上 `--native` 无法内省 `/usr/bin/python3.10`，退到纯 Python 采样 + 每线程 pidstat 补 GIL 证据。注意 `pgrep -f perception_node.py` 会误中 tmux/bash 包装进程，须选 `comm=python3` 的真 PID。
- **非确定性**：VIO 逐次不可复现（定速回放 + 异步感知的时序敏感），所有耗时/占用取多次中位数。
- **工具缺失应对**：节点无 `nsys`/`dcgmi`/`perf`。GPU-context 假设用"同卡多进程 sm% + 每进程延迟 + MPS A/B"替代 nsys 时间线——判定力等价（N≤8 时 CPU 不可能是混淆项）。
- **零 VIO 改动**：`perception_node.py` / `build_map_node.py` / `models_trt.py` 一律未改，只做外部观测与编排；测试全用 raw 集的 scratch 副本，未触碰任何真实产物。

## 附录 B：图表与数据（`profiling/results/`，脚本在 `profiling/scripts/`）

| 图 | 文件 | 一句话 |
|---|---|---|
| 1 | `1_roofline.png` | 观测 1196 vs CPU 2949 / GPU 24000 / IO 66500——离每堵墙都很远 |
| 2 | `2_throughput_vs_par.png` | 峰在 PAR=64，80/96 回退；边际增益在峰处过零 |
| 3 | `3_util_vs_par.png` | 真实 CPU-busy(50%) 与 GPU sm%(7%) 均不饱和 |
| 4 | `4_stage_gantt.png` | 逐帧感知 29s 主导，建图数学仅 1.8s |
| 5 | `5_gpu_concurrency_signature.png` | N=1..8 吞吐线性涨、延迟平 = GPU 能吸收 8 进程 |
| 6 | `6_flame_build.svg` / `6_flame_perception.svg` | build_map 63% 时间在 ROS 图像序列化 |
| 7 | `7_mps_ab.png` | MPS 关 421 vs 开 420 = 否决 |

原始数据（`profiling/results/`）：`footprint_raw.csv`（单集足迹）、`par_sweep_raw.csv`（PAR 扫描）、`concurrency_mps_raw.csv`（同卡 + MPS）、`frames_raw.csv`（帧数分布）、`flamegraph_analysis.txt`（火焰图热点 + GIL）。
可复现脚本（`profiling/scripts/`）：`prof_*.sh` / `prof_*.py`（18 个，一键链路见下；图可用本地 venv 装 matplotlib 从 raw CSV 重渲）。

```
prof_recreate_container.sh   # 容器（16g shm + SYS_PTRACE + 离线装 py-spy）
 → prof_footprint.sh         # 4A 单集足迹（cgroup C / IO / GPU）
 → prof_run_par_sweep.sh     # 4D PAR 扫描（真实 CPU%/sm%）
 → prof_run_gpu_experiments.sh # 4C 同卡并发 + 4E MPS A/B
 → prof_flamegraph.sh        # 4B 火焰图 + 每线程 GIL
 → prof_plots.py             # 渲染 7 张图
```
